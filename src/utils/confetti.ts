/**
 * Labour Day theme: confetti is intentionally disabled in favor of a
 * minimal, professional success modal (checkmark + message). These
 * exports remain as no-ops so existing call sites continue to compile.
 */
export const triggerValentineConfetti = () => {
  /* intentionally minimal — no celebratory particles */
};

export const triggerHeartExplosion = () => {
  /* intentionally minimal — no celebratory particles */
};
